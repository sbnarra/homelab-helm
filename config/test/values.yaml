domain: sbnarra.test
isLive: false

controlPlaneIPs:
- 192.168.1.111

machine_ips:
  cluster: 192.168.1.92
  vm_host: 192.168.1.101

namespace_persistence:
  network:
    source: "{{ .Values.machine_ips.vm_host }}"
  tools:
    source: "{{ .Values.machine_ips.vm_host }}"
  ai:
    source: "{{ .Values.machine_ips.vm_host }}"
  starr:
    source: "{{ .Values.machine_ips.vm_host }}"
  media:
    source: "{{ .Values.machine_ips.vm_host }}"
  printer:
    source: "{{ .Values.machine_ips.vm_host }}"
  system:
    source: "{{ .Values.machine_ips.vm_host }}"
  smart-home:
    source: "{{ .Values.machine_ips.vm_host }}"

entryPoints:
- web
middlewares: []
# - network-x-forwarded-https@kubernetescrd
# - network-traefik-forward-auth@kubernetescrd