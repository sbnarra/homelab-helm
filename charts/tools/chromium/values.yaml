image:
  name: linuxserver/chromium
  tag: latest
env:
  DISABLE_IPV6: true
liveness:
  port: 3000
ports:
  ClusterIP:
    - port: 3000
      subdomain: browser
volumes:
  persisted:
    - /config
gpu:
  enabled: true